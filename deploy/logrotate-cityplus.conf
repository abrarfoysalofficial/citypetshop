# Logrotate config for City Plus Pet Shop deploy logs
# Install: sudo cp deploy/logrotate-cityplus.conf /etc/logrotate.d/cityplus
/var/log/cityplus/deploy.log {
    daily
    rotate 14
    compress
    missingok
    notifempty
    create 0640 cityplus cityplus
}
